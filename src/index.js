const { addPassword, getPasswords } = require('./passwordManager');

// Exemple d'ajout d'un mot de passe
addPassword('example.com', 'mypassword123');

// Exemple de récupération des mots de passe
getPasswords();
